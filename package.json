{
  "name": "taiwan-independence",
  "version": "1.0.0",
  "description": "A project for building mai-tools database.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "format": "prettier --write src/**/*.{ts,tsx}",
    "serve": "npx serve -C -l 8081 external"
  },
  "devDependencies": {
    "@types/node": "^14.18.63",
    "prettier": "^2.0.0",
    "typescript": "^4.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "license": "ISC"
}
